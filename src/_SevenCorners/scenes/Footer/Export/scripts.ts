/**
 * Scripts that Export Comp is dependent on
 */
const initExample = () => {

    const message: string = 'Scripts Init: Example';
    const fooMap = new Map([]);
    console.log(message);
}

export default initExample;